# selenium-supervisor-api

Container Selenium com supervisor controlando o processo Chrome/Selenium sob demanda, agora rodando **Gunicorn em produção**.

## Comandos via Makefile

```bash
make build
make up
make start-selenium
make stop-selenium